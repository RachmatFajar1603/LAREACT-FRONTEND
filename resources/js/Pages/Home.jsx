import Sidebar from "@/Components/Sidebar";
import DefaultLayout from "@/Layouts/DefaultLayout";

const Home = () => {
    return (
        <DefaultLayout>
            <p>dashboard</p>
        </DefaultLayout>
    );
};

export default Home;
